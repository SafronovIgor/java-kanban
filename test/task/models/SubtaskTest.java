package task.models;

import task.service.Managers;
import task.service.TaskManager;

class SubtaskTest {
    private static final TaskManager manager = Managers.getDefault();

//    верьте, что объект Subtask нельзя сделать своим же эпиком;
//    у меня отдельный метод который при создании сабтаски просит эпик
}